import 'package:flutter/material.dart';

class LastNewsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("last news")),
      ),
    );
  }
}
